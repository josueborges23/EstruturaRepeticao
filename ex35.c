#include <stdio.h>

/**
 * Encontrar números primos é uma tarefa difícil.
 * Faça um programa que gera uma lista dos números primos existentes entre 1 e um número
 * inteiro informado pelo usuário.
 * 
 * numero = 10; entre 1 e 10; 
 * numero primo = divisivel por 1 e por ele mesmo
 * 1 - não é primo
 * 2 - é primo
 * 3 - é primo
 * 4 - já não é primo
 * 5 - é primo
 * 6 - não é primo
 * 7 - é primo
 * 8 - não é primo
 * 9 - não é primo
 * 10 - não é primo
 *  
*/

int main(void)
{
    int numero;

    printf("Digite um numero:\n");    
    scanf("%i", &numero);

    int divisores=0;
    
    for (int i = 1; i <= numero; i++)
    {
        for(int j = 1; j < i; j++){
            if(j% ){

            }
        }
    }
    // Se divisores = 2 é primo
    

    

}